
// PDF.js Worker - Stable version
// This worker is loaded from a reliable CDN to avoid import issues
if (typeof importScripts !== 'undefined') {
  importScripts('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.0.379/pdf.worker.min.js');
} else {
  // Fallback for environments where importScripts is not available
  console.warn('importScripts not available, PDF.js worker may not function properly');
}
